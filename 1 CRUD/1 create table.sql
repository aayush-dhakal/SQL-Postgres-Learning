CREATE TABLE cities(
  name VARCHAR(50), -- VARCHAR refers to variable characters meaning it be from length 0 to 50.
  country VARCHAR(50),
  population INTEGER,
  area INTEGER -- INTEGER means number without decimal, ranges from -2147483647 to +2147483647
);